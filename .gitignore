.pio/
.vscode/
.DS_Store

# Vendor-Demo-Firmware-Dump (fremdes Copyright) — nur lokal, nicht veröffentlichen
archive/*.bin

# Local credentials — never commit (template is lib/secrets.h.example)
lib/secrets.h
